server:
  port: ${SERVER_PORT:8083}

spring:
  application:
    name: ms-bff

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${JWT_ISSUER_URI}
          audience: ${JWT_AUDIENCE}

catalogo:
  url: ${CATALOGO_URL:http://ms-catalogo:8082}

pedidos:
  url: ${PEDIDOS_URL:http://ms-pedidos:8081}